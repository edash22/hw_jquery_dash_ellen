$( document ).ready(function() {
    var paragraphs = $( "p" );
    var newStyle = {
        color: "blue",
        fontWeight: "bold",
        backgroundColor: "#ddd"
    };

    paragraphs.css( newStyle );
});
